apiVersion: v1
kind: Service
metadata:
    name: frontend-client
    namespace: core
spec:
    type: ClusterIP
    selector:
        app: frontend-client
    ports:
        - name: http
          port: 3000
          targetPort: 3000

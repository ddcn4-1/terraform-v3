apiVersion: v1
kind: Service
metadata:
  labels:
    app: ticketing-system-main
  name: ticketing-main-service
spec:
  type: NodePort
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: ticketing-system-main
status:
  loadBalancer: {}

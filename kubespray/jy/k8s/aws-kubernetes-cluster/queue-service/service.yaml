apiVersion: v1
kind: Service
metadata:
  labels:
    app: ticketing-system-queue
  name: ticketing-queue-service
spec:
  type: NodePort
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: ticketing-system-queue
status:
  loadBalancer: {}

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apps.api.name }}-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.apps.api.name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.apps.api.port }}
      targetPort: {{ .Values.apps.api.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.apps.api.name }}

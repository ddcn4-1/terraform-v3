apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apps.queue.name }}-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Values.apps.queue.name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.apps.queue.port }}
      targetPort: {{ .Values.apps.queue.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.apps.queue.name }}
